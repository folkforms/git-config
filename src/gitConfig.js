const gitConfig = (options) => {
  console.log("gitConfig");

  // FIXME Inputs: Config file and action
  console.log(`options = ${JSON.stringify(options)}`);

  // FIXME Outputs:
  // ...
};

module.exports = gitConfig;
